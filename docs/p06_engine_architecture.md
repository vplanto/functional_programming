# Практика 06: Анатомія рушія (Розбір архітектури Engine у Cyber Tunnel Runner)

> **Декларація курсу.** Академічна доброчесність та авторство матеріалів — у [DISCLAIMER.md](DISCLAIMER.md).  
> 💡 **Концепція наскрізного проєкту:** Цей розбір пояснює, як у проєкті [23_runner](file:///home/pvv/projects/onu/functional_programming/23_runner) поєднуються чисте функціональне ядро, імперативна оболонка симуляції, таксономія MISD та робота під жорстким пресингом часу (Backpressure).

---

## Експрес-опитування та розминка

<details>
<summary>1. Чому наявність циклу while та мутабельних змінних var усередині Engine.scala — це виправданий компроміс, а не архітектурна помилка?</summary>

Це класичний патерн **«Функціональне ядро, імперативна оболонка» (Functional Core, Imperative Shell)**. Рушій симуляції — це закрита кінцева машина станів (State Machine), якій для роботи на швидкості 1 мкс/тік потрібна гранична продуктивність JVM без зайвого навантаження на Garbage Collector. Головне — мутабельність **суворо ізольована** всередині рушія і не просочується назовні в публічні інтерфейси ботів.
</details>

<details>
<summary>2. Чому методи переходу між смугами повертають Option[Lane], а не кидають виняток при виході за край тунелю?</summary>

Кидання винятку (`throw new OutOfBoundsException`) ламає функціональну чистоту, руйнує локальний хід думок та вимагає важкого стек-трейсу в JVM. Тип `Option[Lane]` робить поведінку **чесною та явної на рівні типів**: на крайній лівій смузі лівого сусіда просто не існує (`None`), а рушій елегантно вирішує це через `hero.lane.left.getOrElse(hero.lane)`.
</details>

<details>
<summary>3. Що станеться, якщо бот почне зберігати історію попередніх дій у внутрішньому полі var myHistory: List[Action]?</summary>

Бот втратить властивість **детермінованості та паралельної безпеки (Thread-Safety)**. Його більше не можна буде безпечно перезапустити з того самого сіда, паралелити чи використовувати в розподілених акторах. Увесь необхідний контекст бот уже отримує у незмінному `Observation`.
</details>

---

## 1. Архітектурний кейс: Катастрофа на випробуваннях Fly-by-Wire

У 1990-х роках під час тестування потрійної комп'ютерної системи управління авіалайнера виник збій: три незалежні обчислювальні канали одночасно оцінювали кут нахилу літака.

Один із модулів використовував «розумну» адаптивну фільтрацію з внутрішнім накопиченням історії помилок. На 14-й хвилині польоту через неочікувану турбулентність внутрішній стан цього алгоритму розійшовся з реальною фізикою, і він почав вимагати різкого пікірування.

**Що врятувало літак?**  
Два інші канали були **повністю stateless (безстановими)**: вони обчислювали реакцію виключно на основі поточного пакета датчиків. **Мажоритарний арбітр (2 проти 1)** заблокував небезпечну команду та безпечно посадив літак.

> **Мораль для інженера:**  
> Спільний змінний стан та прихована внутрішня пам'ять в алгоритмах управління — це джерело аварій у критичних системах. Архітектура [23_runner](file:///home/pvv/projects/onu/functional_programming/23_runner) будується за цим самим авіаційним принципом.

---

## 2. «Функціональне ядро, імперативна оболонка» (Core vs Shell)

Проєкт демонструє фундаментальний гібридний баланс сучасної інженерії на Scala:

```
┌──────────────────────────────────────────────────────────┐
│              ІМПЕРАТИВНА ОБОЛОНКА (Engine.scala)          │
│  var hero, var tick, while (hero.alive && tick < max)    │
│  Ізольований state-holder, замір наносекунд, SLA budget  │
│                                                          │
│     ┌──────────────────────────────────────────────┐     │
│     │        ЧИСТЕ ЯДРО (domain.scala)              │     │
│     │  Lane, Height, Stance, Action, TunnelSlice   │     │
│     │  HeroState, Observation (Незмінні case-класи)│     │
│     │                                              │     │
│     │   decide: Observation => Action              │     │
│     │   (Чиста, математично детермінована функція) │     │
│     └──────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

### Чому в Engine допустимий `var`:
1. **Ізоляція мутацій:** Змінні `var hero`, `var tick`, `var coinsCount` замкнені всередині одного методу `run()`. Жоден зовнішній клас не може змінити їх ззовні.
2. **Нульовий оверхед алокацій:** Забіг на 100 000 тіків у режимі бенчмарку з частотою 1 мкс не повинен спамити мільйони непотрібних стек-фреймів рекурсії, навантажуючи збирач сміття (GC).
3. **Чистота з точки зору клієнта:** Ззовні клас `Engine` виглядає як чистий чорний ящик: `engine.run()` повертає незмінний `RunSummary`.

---

## 3. Доменний дизайн у `domain.scala`: Алгебра типів

Домен проєкту використовує виразну силу системи типів Scala 3:

### А. Моделювання простору через закриті `enum`
```scala
enum Lane:
  case Left, Center, Right

  def left: Option[Lane] = this match
    case Right  => Some(Center)
    case Center => Some(Left)
    case Left   => None

  def right: Option[Lane] = this match
    case Left   => Some(Center)
    case Center => Some(Right)
    case Right  => None
```
* **Exhaustiveness checking:** Компілятор гарантує, що студент не забуде розглянути будь-яку смугу в `match`.
* **Чесні переходи через `Option`:** Замість магічних чисел `-1` чи помилок, рушій безпечно виражає межі:
  ```scala
  val nextLane = action match
    case Action.MoveLeft  => hero.lane.left.getOrElse(hero.lane)
    case Action.MoveRight => hero.lane.right.getOrElse(hero.lane)
    case _                => hero.lane
  ```

### Б. Модель зрізу тунелю (3x3 Matrix)
```scala
case class TunnelSlice(cells: Map[(Lane, Height), CellItem]):
  def itemAt(lane: Lane, height: Height): CellItem =
    cells.getOrElse((lane, height), CellItem.Empty)
```
Зріз простору моделюється як чиста функція-відображення `(Lane, Height) => CellItem`. Простір дискретний, фіксований і детермінований.

---

## 4. Таксономія MISD та Мажоритарний Арбітр (`MajorityBot`)

Концепція **MISD (Multiple Instruction, Single Data)** полягає в тому, що **один незмінний об'єкт `Observation`** одночасно передається різним незалежним алгоритмам:

```scala
override def decide(observation: Observation): Action =
  // 1. Паралельне опитування незалежних ботів (MISD)
  val votes: List[Action] = bots.map(_.decide(observation))

  // 2. Функціональне групування та підрахунок голосів
  val counts: Map[Action, Int] = 
    votes.groupBy(identity).view.mapValues(_.size).toMap

  // 3. Знаходження лідера голосування
  val (winnerAction, maxVotes) = counts.maxBy(_._2)

  // 4. Мажоритарний кворум (2 з 3 або 3 з 3)
  if maxVotes >= 2 then winnerAction
  else votes.headOption.getOrElse(Action.KeepRunning)
```

### Як працює підрахунок голосів під капотом (Покрокове трасування)
Уявімо, що три наші боти повернули такі дії: `List(Action.Jump, Action.Jump, Action.MoveLeft)`.

* **Крок 1. `votes.groupBy(identity)`** — групує однакові дії у кошики:
  $$\text{Map}(\text{Jump} \to \text{List}(\text{Jump}, \text{Jump}), \; \text{MoveLeft} \to \text{List}(\text{MoveLeft}))$$
* **Крок 2. `view.mapValues(_.size).toMap`** — ліниво замінює списки на їхню довжину (кількість голосів):
  $$\text{Map}(\text{Jump} \to 2, \; \text{MoveLeft} \to 1)$$
* **Крок 3. `counts.maxBy(_._2)`** — знаходить пару з найбільшим значенням:
  $$(\text{Jump}, 2)$$
* **Крок 4. Мажоритарний кворум ($2 \ge 2$):** дія `Jump` перемагає більшістю голосів!

---

## 5. Зони когнітивного опору та пастки для студентів

Під час розробки власних ботів студенти гарантовано зіткнуться з трьома пастками. Завдання викладача — розібрати їх на практиці:

```
   ┌─────────────────────────────────────────────────────────────┐
   │                  ТРИ ПАСТКИ ДЛЯ СТУДЕНТА                    │
   ├──────────────────────────────┬──────────────────────────────┤
   │ 1. Пастка стану (Stateful)   │ Бажання додати `var` у бота  │
   │ 2. Часова зв'язність         │ Непрохідність зв'язки N->N+1 │
   │ 3. Експоненційний таймаут    │ Повний перебір O(N) без відсіч│
   └──────────────────────────────┴──────────────────────────────┘
```

### Пастка 1: Бажання зберегти стан (Stateful Bot)
* **Симптом:** Студент додає поле `var previousAction: Action` або `var jumpsCount: Int` у клас свого бота.
* **Чому це глухий кут:** Бот має бути **чистою функцією**. Весь контекст уже присутній у `observation.hero` (дистанція, поточна смуга, стійка `stance`, рахунок). Бот, що має внутрішній стан, стає непередбачуваним при повторному тестуванні чи зміні порядку опитування в ансамблі.

### Пастка 2: Часова зв'язність зрізів ($N \to N+1$)
* **Симптом:** Генератор рівнів [LevelGenerator.scala](file:///home/pvv/projects/onu/functional_programming/23_runner/src/main/scala/runner/LevelGenerator.scala) перевіряє прохідність кожного зрізу окремо (`isSolvable`), але в реальній грі дія на кроці $t$ звужує простір дій на кроці $t+1$.

#### Схема обмеження фізики між тактами часу:
```
Тік t:   [Герой на землі (Running)] ───Дія: Jump───► Тік t+1: [Герой у повітрі (Jumping)]
                                                               │
                                                       Стеля:  НЕБЕЗПЕКА (High [2] вбиває миттєво!)
                                                       Заборона: Duck (неможливо пригнутися в польоті)
                                                       Заборона: Jump (подвійні стрибки не підтримуються)
```

* **Приклад пастки:**
  * На кроці $N$ на землі бар'єр `Low [0]`. Єдиний вихід — стрибок `Jump`.
  * Тоді на кроці $N+1$ герой **фізично опиниться в повітрі** (`Stance.Jumping`).
  * Якщо на кроці $N+1$ на тій самій смузі стоїть балка під стелею `High [2]`, герой розіб'ється, бо не може присісти в польоті!
* **Методичний висновок:** `StrategicBot` повинен будувати міні-дерево переходів станів (State Machine), де вузлами графа є не просто смуги, а **пари `(Lane, Stance)`** з урахуванням фізичних обмежень переходу між сусідніми тактами часу.

### Пастка 3: Боротьба з експоненційним Backpressure у режимі Benchmark
* **Симптом:** У режимі `benchmark` бюджет на тік падає за формулою $100\text{ мс} \times e^{-t/50}$ до $500\text{ нс}$.
* **Чому наївний алгоритм розбивається:** Повний перебір траєкторій дерева глибиною 8 кроків має складність $O(3^8) \approx 6561$ операцій. При бюджеті у $10$ мікросекунд такий перебір не встигає виконатися, бот ловить Timeout і застосовує аварійний `KeepRunning`.
* **Функціональне вирішення:** Студенти повинні навчитися перемикати евристики:
  * Рефлекси: $O(1)$ — миттєва реакція на 1-й зріз.
  * Жадібний вибір: $O(K)$ — оцінка тільки безпечних монет.
  * Стратег: перевіряє `observation.timeBudgetNanos` і динамічно зменшує глибину пошуку при падінні ліміту часу.

---

## 6. Напрямки інженерного вдосконалення коду

Для відмінних оцінок та науково-дослідних тем рекомендується розібрати два покращення:

### 1. Pure State-Passing Random
Зараз генерація чисел використовує мутабельний `scala.util.Random`. Чистий підхід у стилі книги *Functional Programming in Scala (Red Book)*:
```scala
case class SimpleRNG(seed: Long):
  def nextInt: (Int, SimpleRNG) =
    val newSeed = (seed * 0x5DEECE66DL + 0xBL) & 0xFFFFFFFFFFFFL
    val nextRNG = SimpleRNG(newSeed)
    val n = (newSeed >>> 16).toInt
    (n, nextRNG)
```

> 💡 **Підказка для студентів: Як прокинути нове зерно `SimpleRNG` без `var`?**  
> Справжній виклик полягає у питанні: *«Як зберегти нове зерно `SimpleRNG` між тіками без мутабельних змінних?»*  
> У чистому функціональному програмуванні такий стан передається як **наскрізний рекурсивний акумулятор**:
> ```scala
> def gameLoop(tick: Long, hero: HeroState, rng: SimpleRNG): RunSummary =
>   val (action, nextRNG) = bot.decidePure(observation, rng)
>   val nextHero = applyPhysics(hero, action, currentSlice)
>   gameLoop(tick + 1, nextHero, nextRNG)
> ```
> Кожна ітерація викликає наступну, передаючи свіже `nextRNG`. На старших курсах ви побачите, що цей патерн абстрагується у **монаду `State[SimpleRNG, Action]`**, яка приховує «проводку» стану всередині `for`-comprehension.

### 2. Покомпонентна телеметрія ансамблю
Розширення `TickRecord` фіксацією часу кожного з трьох ботів окремо:
`subBotTimings: Map[String, Long]`. Це дозволяє у [index.html](file:///home/pvv/projects/onu/functional_programming/docs/viewer/index.html) бачити графік «Хто з трійки ботів винен у таймауті».

---

## 7. Контрольні питання для самоперевірки

<details>
<summary>1. Чому функція decide(observation: Observation) повинна повертати дію для НАСТУПНОГО тіка (t+1), а не для поточного (t)?</summary>

Тому що на тіку $t$ герой уже займає певну смугу та стійку, взаємодіючи зі зрізом $t$. Щоб змінити смугу або підстрибнути над бар'єром на зрізі $t+1$, імпульс руху повинен бути розрахований і переданий моторам заздалегідь під час такту $t$.
</details>

<details>
<summary>2. Чим відрізняється поведінка Option[Lane].getOrElse від конструкції if/else?</summary>

`Option` виражає відсутність значення на рівні системи типів, позбавляючи від магічних констант і перевірок на `null`. `getOrElse` — це ідіоматичний вираз безпечного fallback: якщо повернуто `None`, використовується значення за замовчуванням без ризику `NullPointerException`.
</details>

<details>
<summary>3. Яка обчислювальна складність у ланцюжка MajorityBot: map -> groupBy -> view.mapValues -> maxBy?</summary>

Оскільки кількість ботів фіксована ($N = 3$), а кількість можливих дій скінченна ($M = 5$), складність мажоритарного вибору становить константний час $O(1)$. Це ідеально підходить для умов жорсткого Backpressure.
</details>
